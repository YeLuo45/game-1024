# Technical Solution — P-20260502-007/008

## 概述

game-1024 V2 迭代，包含两个独立功能模块：

- **P-20260502-007**：2048 模式 / 无限模式切换
- **P-20260502-008**：每日挑战 + 成就系统

技术栈继承 V1：React 18 + Vite 5 + vite-plugin-pwa + localStorage

---

## 技术方案 — P-20260502-007（2048/无限模式切换）

### 1. 模式配置

```js
// src/utils/modes.js
export const MODES = {
  '2048': {
    key: 'game-1024-state-2048',
    winValue: 2048,
    hasGameOver: true,  // 无法移动时结束
  },
  'infinite': {
    key: 'game-1024-state-infinite',
    winValue: null,     // 无目标上限
    hasGameOver: true,  // 无法移动时结束
  }
};
```

### 2. 模式状态管理

在 `useGame.js` 中新增 mode 参数，根据当前模式动态调整：

- `WIN_VALUE`：2048 模式为 2048，无限模式为 null（不触发胜利）
- 存档 key：`game-1024-state-2048` / `game-1024-state-infinite`
- 无限模式：合并后继续玩，不设目标格

### 3. 模式切换 UI

在 `App.jsx` 右上角菜单新增「模式切换」入口：

- 切换前提示「切换模式将重新开始」
- 确认后调用 `newGame()` 并切换存档 key
- 当前模式持久化到 `game-1024-mode`（下次进入默认上次模式）

### 4. 存档结构

```js
// 2048 模式
{ grid: number[][], score: number, won: boolean, gameOver: boolean }

// 无限模式
{ grid: number[][], score: number, bestTile: number, gameOver: boolean }
```

---

## 技术方案 — P-20260502-008（每日挑战 + 成就系统）

### 1. 每日挑战

#### 1.1 初始盘面生成

```js
// src/utils/daily.js

// 以日期为 seed，保证同一天全球相同盘面
function getDailySeed(date = new Date()) {
  const str = date.toISOString().split('T')[0]; // "2026-05-02"
  let hash = 0;
  for (let i = 0; i < str.length; i++) {
    hash = ((hash << 5) - hash) + str.charCodeAt(i);
    hash |= 0;
  }
  return Math.abs(hash);
}

// 生成固定盘面（4x4，2-4个随机方块）
function generateDailyGrid(date) {
  const seed = getDailySeed(date);
  const rng = seededRandom(seed);
  const g = createEmptyGrid();
  const count = rng() < 0.5 ? 2 : 3;
  for (let i = 0; i < count; i++) {
    // 放置方块...
  }
  return g;
}
```

#### 1.2 存档结构

```js
// localStorage key: game-1024-daily
{
  date: "2026-05-02",       // 最后游玩日期
  bestScore: 12340,          // 今日最高分
  playedToday: true,         // 今日是否已玩
  history: {                 // 历史记录（可选）
    "2026-05-01": 15000,
    "2026-05-02": 12340
  }
}
```

#### 1.3 重置逻辑

每次进入每日挑战时检查 `date` 是否为今天，不是则重置。

### 2. 成就系统

#### 2.1 成就定义

```js
// src/utils/achievements.js
export const ACHIEVEMENTS = [
  { id: 'first-merge',   name: '初试锋芒',    desc: '完成第一次合并',     reward: 10 },
  { id: 'reach-128',     name: '小有所成',     desc: '达到 128',           reward: 10 },
  { id: 'reach-512',     name: '中级目标',     desc: '达到 512',           reward: 10 },
  { id: 'reach-1024',    name: '达成目标',     desc: '达到 1024',          reward: 10 },
  { id: 'reach-2048',    name: '登峰造极',    desc: '达到 2048',          reward: 10 },
  { id: 'reach-4096',    name: '超越极限',    desc: '达到 4096',          reward: 10 },
  { id: 'combo-3',       name: '三连击',       desc: '连续3次合并',        reward: 10 },
  { id: 'no-dead-50',    name: '步步为营',     desc: '前50步无死亡',       reward: 10 },
  { id: 'daily-3',       name: '连续三日',     desc: '完成3天每日挑战',     reward: 10 },
  { id: 'all-skins',     name: '皮肤收集者',   desc: '使用过全部皮肤',      reward: 10 },
];
```

#### 2.2 检测点

| 检测点 | 触发条件 |
|--------|---------|
| 合并时 | 检测 `first-merge` / `combo-3` / `reach-*` |
| 达到某格值 | 检测 `reach-128/512/1024/2048/4096` |
| 游戏结束时 | 检测 `no-dead-50`（胜利时且步数<50） |
| 皮肤切换时 | 检测 `all-skins` |
| 每日挑战重置时 | 检测 `daily-3` |

#### 2.3 成就存档

```js
// localStorage key: game-1024-achievements
{
  unlocked: ['first-merge', 'reach-128'],  // 已解锁ID列表
  unlockDates: {                           // 解锁日期
    'first-merge': '2026-05-02',
    'reach-128': '2026-05-02'
  }
}
```

#### 2.4 解锁弹窗

在 `Game.jsx` 或新增 `AchievementPopup.jsx` 组件：

- 居中弹出，显示成就名称 + 描述 + 奖励
- 2秒后自动消失
- 每次游戏只弹一次（避免连续触发）

---

## 文件改动清单

| 文件 | 改动 |
|------|------|
| `src/utils/modes.js` | 新增，模式配置 |
| `src/utils/daily.js` | 新增，每日挑战逻辑 |
| `src/utils/achievements.js` | 新增，成就定义 |
| `src/hooks/useGame.js` | 修改，支持模式参数、独立存档 |
| `src/hooks/useDaily.js` | 新增，每日挑战存档 |
| `src/hooks/useAchievements.js` | 新增，成就解锁检测 |
| `src/components/Game.jsx` | 修改，集成成就检测、每日挑战入口 |
| `src/components/ModeSwitcher.jsx` | 新增，模式切换 UI |
| `src/components/AchievementPopup.jsx` | 新增，成就解锁弹窗 |
| `src/components/DailyChallenge.jsx` | 新增，每日挑战页面 |
| `src/components/Achievements.jsx` | 新增，成就列表页面 |
| `src/App.jsx` | 修改，新增菜单入口 |

---

## 验收标准

- [ ] 2048 模式和无限模式可自由切换，存档独立
- [ ] 无限模式无目标上限，无法移动时 Game Over
- [ ] 每日挑战日期正确，固定盘面可复现
- [ ] 10 个成就全部可解锁，解锁弹窗正常显示
- [ ] 成就存档不丢失
- [ ] npm run build 成功
- [ ] 控制台无 Error
